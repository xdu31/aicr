# Copyright (c) 2026, NVIDIA CORPORATION & AFFILIATES.  All rights reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

# GPU Operator OCP ClusterPolicy values
#
# Structure mirrors gpu-operator/values.yaml for side-by-side comparison.
# The template maps these flat keys into the ClusterPolicy CR spec.

name: cluster-policy

operator:
  defaultRuntime: crio

dcgm:
  enabled: true

toolkit:
  enabled: true
  installDir: /usr/local/nvidia
  env:
    - name: ACCEPT_NVIDIA_VISIBLE_DEVICES_ENVVAR_WHEN_UNPRIVILEGED
      value: "false"
    - name: ACCEPT_NVIDIA_VISIBLE_DEVICES_AS_VOLUME_MOUNTS
      value: "true"

dcgmExporter:
  enabled: true
  serviceMonitor:
    enabled: true
  config:
    name: dcgm-exporter
    data: |
      # Clocks,,
      DCGM_FI_DEV_SM_CLOCK,     gauge, SM clock frequency (in MHz).
      DCGM_FI_DEV_MEM_CLOCK, gauge, Memory clock frequency (in MHz).

      # Temperature,,
      DCGM_FI_DEV_MEMORY_TEMP, gauge, Memory temperature (in C).
      DCGM_FI_DEV_GPU_TEMP,    gauge, GPU temperature (in C).

      # Power,,
      DCGM_FI_DEV_POWER_USAGE,  gauge, Power draw (in W).
      DCGM_FI_DEV_TOTAL_ENERGY_CONSUMPTION, counter, Total energy consumption since boot (in mJ).

      # PCIe,,
      DCGM_FI_DEV_PCIE_REPLAY_COUNTER, counter, Total number of PCIe retries.

      # Utilization (the sample period varies depending on the product),,
      DCGM_FI_DEV_GPU_UTIL,      gauge, GPU utilization (in %).
      DCGM_FI_DEV_MEM_COPY_UTIL, gauge, Memory utilization (in %).
      DCGM_FI_DEV_ENC_UTIL,      gauge, Encoder utilization (in %).
      DCGM_FI_DEV_DEC_UTIL,      gauge, Decoder utilization (in %).

      # Errors and violations,,
      DCGM_FI_DEV_XID_ERRORS,            gauge, Value of the last XID error encountered.
      DCGM_FI_DEV_POWER_VIOLATION,       counter, Throttling duration due to power constraints (in us).
      DCGM_FI_DEV_THERMAL_VIOLATION,     counter, Throttling duration due to thermal constraints (in us).
      DCGM_FI_DEV_SYNC_BOOST_VIOLATION,  counter, Throttling duration due to sync-boost constraints (in us).
      DCGM_FI_DEV_BOARD_LIMIT_VIOLATION, counter, Throttling duration due to board limit constraints (in us).
      DCGM_FI_DEV_LOW_UTIL_VIOLATION,    counter, Throttling duration due to low utilization (in us).
      DCGM_FI_DEV_RELIABILITY_VIOLATION, counter, Throttling duration due to reliability constraints (in us).

      # Memory usage,,
      DCGM_FI_DEV_FB_FREE, gauge, Framebuffer memory free (in MiB).
      DCGM_FI_DEV_FB_USED, gauge, Framebuffer memory used (in MiB).

      # Retired pages,,
      DCGM_FI_DEV_RETIRED_SBE,     counter, Total number of retired pages due to single-bit errors.
      DCGM_FI_DEV_RETIRED_DBE,     counter, Total number of retired pages due to double-bit errors.
      DCGM_FI_DEV_RETIRED_PENDING, counter, Total number of pages pending retirement.

      # NVLink,,
      DCGM_FI_DEV_NVLINK_BANDWIDTH_TOTAL, counter, Total number of NVLink bandwidth counters for all lanes
      DCGM_FI_PROF_NVLINK_TX_BYTES,       counter, The rate of data transmitted over NVLink not including protocol headers in bytes per second.
      DCGM_FI_PROF_NVLINK_RX_BYTES,       counter, The rate of data received over NVLink not including protocol headers in bytes per second.

      # Add DCP metrics,,
      DCGM_FI_PROF_GR_ENGINE_ACTIVE,   gauge, Ratio of time the graphics engine is active (in %).
      DCGM_FI_PROF_SM_ACTIVE,          gauge, The ratio of cycles an SM has at least 1 warp assigned (in %).
      DCGM_FI_PROF_SM_OCCUPANCY,       gauge, The ratio of number of warps resident on an SM (in %).
      DCGM_FI_PROF_PIPE_TENSOR_ACTIVE, gauge, Ratio of cycles the tensor (HMMA) pipe is active (in %).
      DCGM_FI_PROF_DRAM_ACTIVE,        gauge, Ratio of cycles the device memory interface is active sending or receiving data (in %).
      DCGM_FI_PROF_PCIE_TX_BYTES,      counter, The number of bytes of active pcie tx data including both header and payload.
      DCGM_FI_PROF_PCIE_RX_BYTES,      counter, The number of bytes of active pcie rx data including both header and payload.

      # BCP Additional metrics
      DCGM_FI_DEV_GPU_NVLINK_ERRORS,      gauge, Identifies a GPU NVLink error type returned by DCGM_FI_DEV_GPU_NVLINK_ERRORS.

      # Added RunAI Additional metrics https://docs.run.ai/latest/developer/metrics/metrics-api/#advanced-metrics
      ## NVLink
      DCGM_FI_DEV_NVLINK_BANDWIDTH_L0, counter, The number of bytes of active NVLink rx or tx data including both header and payload.
      ## VGPU License status
      DCGM_FI_DEV_VGPU_LICENSE_STATUS, gauge, vGPU License status
      ## Remapped rows
      DCGM_FI_DEV_UNCORRECTABLE_REMAPPED_ROWS, counter, Number of remapped rows for uncorrectable errors
      DCGM_FI_DEV_CORRECTABLE_REMAPPED_ROWS, counter, Number of remapped rows for correctable errors
      DCGM_FI_DEV_ROW_REMAP_FAILURE, gauge, Whether remapping of rows has failed
      ## Static configuration information. These appear as labels on the other metrics
      DCGM_FI_DRIVER_VERSION, label, Driver Version
      ## Profiling metrics
      DCGM_FI_PROF_PIPE_FP64_ACTIVE, gauge, Ratio of cycles the fp64 pipes are active (in %).
      DCGM_FI_PROF_PIPE_FP32_ACTIVE, gauge, Ratio of cycles the fp32 pipes are active (in %).
      DCGM_FI_PROF_PIPE_FP16_ACTIVE, gauge, Ratio of cycles the fp16 pipes are active (in %).

gdrcopy:
  enabled: true

gfd:
  enabled: true

driver:
  enabled: true
  rdma:
    enabled: true
    useHostMofed: false

# OCP platform-level setting: cri-o has native CDI support and it is the
# recommended device injection path. The Helm chart controls this via the
# driver sub-chart — no direct equivalent in gpu-operator/values.yaml.
cdi:
  enabled: true
  default: true

devicePlugin:
  enabled: true

migManager:
  enabled: true

# OCP-only: not present in the Helm chart values.
sandboxWorkloads:
  enabled: false

# OCP-only: not present in the Helm chart values.
nodeStatusExporter:
  enabled: true

# OCP-only: ClusterPolicy daemonset rollout policy.
# The Helm chart controls this via operator.upgradeCRD / chart-level settings.
daemonsets:
  tolerations:
    - operator: Exists
  rollingUpdate:
    maxUnavailable: '1'
  updateStrategy: RollingUpdate

# --- Fields present in gpu-operator/values.yaml but NOT in ClusterPolicy CR ---
# hostPaths.driverInstallDir: Helm chart setting, not a ClusterPolicy field.
# operator.upgradeCRD: Helm chart setting, not a ClusterPolicy field.
# operator.resources: Helm chart setting for the operator Deployment, not CR.
# driver.version: OCP operator manages driver version via the certified driver container.
# driver.useOpenKernelModules: OCP uses pre-built driver containers from the certified catalog.
# driver.maxParallelUpgrades: Use daemonsets.rollingUpdate.maxUnavailable instead.
# devicePlugin.env: OCP sets device plugin env via the operator's own defaults.
# validator: OCP operator manages validation internally.
# nfd: OCP deploys NFD via the nfd-ocp component, not as a gpu-operator sub-chart.
# ccManager: OCP operator manages CC via its own reconciliation.
# fullnameOverride: Helm chart setting, not a ClusterPolicy field.
